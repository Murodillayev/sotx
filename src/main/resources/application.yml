spring:
  shell:
    interactive:
      enabled: true

  datasource:
    username: macbookpro
    password: 19801980
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/test

  jpa:
    hibernate:
      ddl-auto: update